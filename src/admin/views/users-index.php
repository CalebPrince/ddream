<?php declare(strict_types=1); $headerAction='<a class="btn btn-primary" href="'.e(admin_url('users/new')).'">Add user</a>'; ?>
<div class="mb-5 flex justify-end"><a class="btn btn-primary" href="<?= e(admin_url('users/new')) ?>">Add user</a></div>
<div class="card overflow-hidden"><div class="divide-y divide-hairline"><?php foreach($rows as $row): ?><a href="<?= e(admin_url('users/'.$row['id'])) ?>" class="flex flex-wrap items-center gap-4 px-5 py-4 hover:bg-canvas"><span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-navy-100 font-semibold text-navy-700"><?= e(mb_strtoupper(mb_substr($row['name'],0,1))) ?></span><div class="min-w-[13rem] flex-1"><p class="font-semibold text-navy-700"><?= e($row['name']) ?></p><p class="t-meta text-muted"><?= e($row['email']) ?></p></div><span class="badge bg-gold-100 text-gold-600"><?= e(role_label($row['role'])) ?></span><span class="badge <?= $row['status']==='active'?'bg-verified/10 text-verified':'bg-signal-600/10 text-signal-600' ?>"><?= e($row['status']) ?></span><p class="w-36 t-meta text-muted"><?= $row['last_login_at']?'Last seen '.e(time_ago($row['last_login_at'])):'Never signed in' ?></p></a><?php endforeach; ?></div></div>
